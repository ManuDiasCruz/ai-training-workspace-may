server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: route everything that isn't a real file to index.html
    # so client-side routing (/top, /random, ...) works on direct navigation.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
