server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Long-cache hashed static assets.
    location /static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Single-page-app fallback: serve index.html for client-side routes.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
