# PostgreSQL connection string used by Prisma at runtime
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/singmeasong?schema=public

# HTTP port the API listens on (defaults to 5000)
PORT=5000

# Set to TEST to enable the /tests route used by the e2e/integration suites
# MODE=TEST
