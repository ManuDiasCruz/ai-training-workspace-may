# PostgreSQL connection string used by Prisma (development database)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sing_me_a_song

# Port the HTTP server listens on
PORT=5000

# Set to TEST to expose the /tests router (reset/seed helpers used by Cypress).
# Leave unset in production.
# MODE=TEST
