# PostgreSQL connection string used by Prisma.
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/sing_me_a_song

# API listen port.
PORT=5000

# Comma-separated browser origins allowed by CORS.
CORS_ORIGIN=http://localhost:3000

# Set MODE=TEST only when enabling the /tests helper routes locally.
MODE=DEVELOPMENT
