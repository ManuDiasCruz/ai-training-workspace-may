# Port the API listens on during tests
PORT=5000

# Enables the /tests/reset route used by E2E tests
MODE=TEST

# Separate database so tests can truncate tables safely
DATABASE_URL=postgres://postgres:postgres@localhost:5432/singmeasong_test
