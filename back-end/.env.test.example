# Used by `npm test`, `npm run dev:test` and Cypress. Point it to a THROWAWAY database:
# the test suite truncates and resets it.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sing_me_a_song_test
PORT=5000
MODE=TEST
