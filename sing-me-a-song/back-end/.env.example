# Local development only. Replace placeholders; never commit .env.
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/sing_me_a_song
PORT=5000
CORS_ORIGIN=http://localhost:3000
# Production: set NODE_ENV=production in the hosting dashboard.
# Never enable test routes in production.
