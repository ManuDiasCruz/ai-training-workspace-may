# Local example only. Supply a real private connection URL in production.
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/sing_me_a_song
PORT=5000
NODE_ENV=development
# Comma-separated frontend origins; leave unset for same-origin production.
CORS_ORIGINS=http://localhost:3000
