# Copy to .env.test — used by the automated test scripts (npm test, npm run dev:test)
# Point it at a DISPOSABLE database: tests truncate the recommendations table.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sing_me_a_song_test

PORT=5000

# Exposes DELETE /tests/reset so E2E tests can clean the database
MODE=TEST
