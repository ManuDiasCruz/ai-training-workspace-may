# Dedicated disposable database. NEVER point this at your normal database.
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/sing_me_a_song_test
NODE_ENV=test
PORT=5000
# Only needed for Cypress's destructive reset route, disabled by default.
ENABLE_TEST_ROUTES=false
